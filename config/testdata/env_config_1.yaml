id: env-config-1
proxy_configs:
- basepath: /v1
  authentication:
    jwt:
      name: foo
      issuer: bar
      in:
      - header: header
      remote_jwks:
        url: url
        cache_duration: 1h
  consumer_authorization:
    in:
      header: x-api-key
  target: my-target
  operations:
  - name: op-1
    http_match:
    - path_template: /petstore
      method: GET
